#include<stdio.h>
#include<conio.h>
int main()
{
 int charcter,i;
 string s[200];
 character = 0;
 printf("enter a sentence");
 scanf(""%s",s);
 for(i=0;i<s[i];i++)
 {
  if(s[i]=="a" && s[i]=="z)||(s[i]=="A" && s[i]=="Z")
  {
      character++;
   }
   }
   printf("%s,%d characters);
   return 0;
   }
  
 
  
 
